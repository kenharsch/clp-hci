<%= form_for(@show_name) do |f| %>
  <% if @show_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@show_name.errors.count, "error") %> prohibited this show_name from being saved:</h2>

      <ul>
      <% @show_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
